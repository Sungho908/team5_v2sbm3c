<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dev.mvc.likes.LikesDAOInter">

  <insert id="create">
    INSERT INTO likes(likesno, reviewno)
    VALUES(likes_seq.nextval, review_seq.currval)
  </insert>

  <update id="increased_likes" parameterType="Integer">
    UPDATE likes
    SET likes =  likes + 1
    WHERE likesno = #{reviewno}
  </update>

  <update id="decreased_likes" parameterType="Integer">
    UPDATE likes
    SET likes =  likes - 1
    WHERE likesno = #{reviewno}
  </update>

  <update id="increased_hates" parameterType="Integer">
    UPDATE likes
    SET hates =  hates + 1
    WHERE likesno = #{reviewno}
  </update>

  <update id="decreased_hates" parameterType="Integer">
    UPDATE likes
    SET hates =  hates - 1
    WHERE likesno = #{reviewno}
  </update>

  <delete id="delete" parameterType="Integer">
    DELETE FROM likes
    WHERE likesno = #{reviewno}
  </delete>

</mapper>