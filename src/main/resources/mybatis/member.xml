<?xml version="1.0" encoding="UTF-8"?>
 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dev.mvc.member.MemberDAOInter" >
 <!-- resultType=""  parameterType="" -->
  <insert id="create" parameterType="dev.mvc.member.MemberVO" >
    INSERT INTO member(m_no, m_id, m_pw, m_name, m_nickname, m_phone, m_email, m_thumb, m_addr1, m_addr2, m_zipcode, m_date, m_rdate, m_point, m_gender, m_grade, m_role)
    VALUES (member_seq.nextval, #{mId}, #{mPw}, #{mName}, #{mNickname}, #{mPhone}, #{mEmail}, #{mThumb}, #{mAddr1}, #{mAddr2}, #{mZipcode}, #{mDate}, sysdate, 0, #{mGender}, 1, 'USER')
  </insert>
  
  <select id="checkId" parameterType="String" resultType="int">
    SELECT COUNT(m_id) as cnt
    FROM member
    WHERE m_id=#{id}
  </select>
  
  <select id="readById" parameterType="String" resultType="dev.mvc.member.MemberVO">
    SELECT m_no, m_id, m_pw, m_name, m_nickname, m_phone, m_email, m_thumb, m_addr1, m_addr2, m_zipcode, m_date, m_rdate, m_point, m_gender, m_grade, m_role
    FROM member
    WHERE m_id=#{id}
  </select>
</mapper>
