<?xml version="1.0" encoding="UTF-8"?>
 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dev.mvc.shoes.ShoesDAOInter"> <!-- SB가 자동으로 구현하여 연동하는 interface -->
  <insert id="create" parameterType="dev.mvc.shoes.ShoesVO"> <!-- public int create(ShoesVO shoesVO) {...-->
    INSERT INTO shoes(shoesno, name, namesub, cnt, rdate, seqno, visible)
    VALUES(CATE_SEQ.nextval, #{name}, #{namesub}, 0, sysdate, 1, 'Y')
  </insert>
  
  <!-- SQL -> ShoesVO -> ArrayList<ShoesVO> return -->
  <select id="list_all" resultType="dev.mvc.shoes.ShoesVO">
    SELECT shoesno, name, namesub, cnt, rdate, seqno, visible
    FROM shoes
    ORDER BY name ASC, namesub ASC
  </select>

  <!-- SQL -> ShoesVO return -->
  <select id="read" resultType="dev.mvc.shoes.ShoesVO" parameterType="int">
    SELECT shoesno, name, namesub, cnt, rdate, seqno, visible
    FROM shoes
    WHERE shoesno=#{shoesno}
  </select>
  
  <!-- 수정, return: 수정한 레코드 갯수, parameterType: 수정할 레코드 정보 -->
  <update id="update" parameterType="dev.mvc.shoes.ShoesVO">
    UPDATE shoes
    SET name=#{name}, namesub=#{namesub}, cnt=#{cnt}, rdate=sysdate, seqno=#{seqno}, visible=#{visible}
    WHERE shoesno=#{shoesno}
  </update>
  
  <delete id="delete" parameterType="Integer">
    DELETE FROM shoes WHERE shoesno=#{shoesno}
  </delete>
  
</mapper>










